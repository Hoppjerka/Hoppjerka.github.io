version: '3'
services:
  jekyll-serve:
    image: jekyll/jekyll:3.8
    volumes:
      - "..:/srv/jekyll/"
    ports:
      - 4000:4000
    command: 'jekyll serve --config ./build/_config.yml'
#    command: 'jekyll serve --config _config.yml --source ././src --destination ././dist'